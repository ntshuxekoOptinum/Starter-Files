#Use this script to make a call to a local Ollama model
import requests

def classifyCommandOllama(user_text):
    prompt = 
    response = requests.post

    result = response.json()
    return result["response"].strip()


